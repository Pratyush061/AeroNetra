## Stale Performance Optimization: DroneCanvas.tsx\n\nThe reported performance issue regarding `new THREE.Vector3` inside `useFrame` and `vector.unproject(state.camera)` was found to be stale. The current implementation in `src/components/3d/DroneCanvas.tsx` correctly uses module-level pre-allocated scratch objects (`_raySphere`, `_sphereHitTarget`, `_localVector`) to avoid garbage collection overhead per frame, adhering to React-Three-Fiber best practices.\n\nKey Learning: Do not blindly refactor module-level scratch objects into component-level `useMemo` instances for theoretical multi-instance safety without demonstrating actual conflicts, as this introduces unnecessary risk to live production code.
